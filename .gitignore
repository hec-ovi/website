# Static publish mode: keep only exported files for public deployment repo.
*

# Keep this file tracked.
!.gitignore
!README.md
!wrangler.jsonc

# Keep only Next.js static export output.
!out/
!out/**
